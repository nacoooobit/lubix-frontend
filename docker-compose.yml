services:
  frontend:
    build: .
    container_name: lubix-frontend
    ports:
      - "5174:5174"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - VITE_API_URL=http://backend:8000
    command: npm run dev -- --port 5174 --host
